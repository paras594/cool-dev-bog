import React from "react";

const PageLoader = () => {
  return (
    <div className="h-full scale-150 w-full flex items-center justify-center">
      <span className="loading loading-infinity loading-lg"></span>
    </div>
  );
};

export default PageLoader;
